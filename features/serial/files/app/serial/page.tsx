"use client";

/**
 * /serial — Arduino / Web Serial debug page.
 * Coming soon: this will be a script that pulls content from the sheets-cms tool and writes it to the content.json file.
 */
import { useEffect, useRef, useState } from "react";
import { useSerial } from "../../lib/serial/useSerial";

export default function SerialDebugPage() {
  return (
    <main className="mx-auto max-w-3xl">
      <h1 className="mt-1 text-2xl font-bold">Coming Soon - Serial debug page</h1>
    </main>
  );
}
