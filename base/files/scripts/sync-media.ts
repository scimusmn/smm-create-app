/**
 * yarn sync-media
 * Coming Soon: this will be a script that syncs media from a Google Cloud Storage bucket or Google Drive folder to the public/media folder.
 * This is also an example of a script we probably want in every project, which is why it's in the `base` directory... but TBD
 */
