// Placeholder — the CLI overwrites this file at project creation with the
// actual product/feature selection. If you add a feature later with
// `create-smm-app add <feature>`, add its id to `features` here so the
// home page reflects it.
export const templateConfig = {
  product: null as string | null,
  features: [] as string[],
  createdWith: "smm-create-app",
  createdAt: new Date(0).toISOString(),
};
